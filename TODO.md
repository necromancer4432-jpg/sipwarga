# TODO: Fix Bootstrap Class Conflicts

## Tasks
- [x] Rename conflicting custom classes in `assets/Css/warga.css` with `custom-` prefix (e.g., `.btn` to `.custom-btn`)
- [x] Update HTML in `views/warga/riwayat_pengajuan.php` to use new custom class names where custom styles are intended
- [x] Check and update other PHP files that use these custom classes (e.g., data_pribadi.php, pengaturan_warga.php, etc.)
- [x] Verify that Bootstrap classes are used for Bootstrap styling and custom classes for custom styling
- [x] Test the changes to ensure no styling conflicts
